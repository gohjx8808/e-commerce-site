import { styled } from '@mui/material/styles';
import Typography from '@mui/material/Typography';

const TextShadowFont = styled(Typography)({
  textShadow: '0px 1px grey',
});

export default TextShadowFont;
